{
  "masterID": "Application ID of the master bot",
  "masterToken": "Bot token for the master bot",
  "guild": "Guild ID, where roles should be changed",
  "delay": 120000, // delay between role updates in milliseconds 
  "roles": [
    {
      "roleID": "Role ID",
      "alias": "Name,  which will be received from the slash command (should be the same in AddChoice)",
      "workerToken": "Bot token for the worker, which will update this role",
      "colors": [ // HEX colors
        "#ff0000",
        "#00ff00",
        "#0000ff"
      ]
    }
  ]
}
